# Configuration for the summarization feature

# Default model to use for summarization
DEFAULT_MODEL = "openai/gpt-4.1-nano"

# Maximum tokens for summary output
MAX_SUMMARY_TOKENS = 1000

# Maximum characters to send to the API for summarization
MAX_INPUT_CHARS = 15000

# Timeout for API calls in seconds
API_TIMEOUT = 30

# Model is now configured via DEFAULT_MODEL only
# No dropdown selection in the UI

# Minimum length (in characters) for content before summarization is recommended
MIN_SUMMARIZATION_LENGTH = 500
